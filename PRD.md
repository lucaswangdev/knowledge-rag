# Knowledge-RAG 产品需求文档 (PRD)

## 一、项目概述

### 1.1 项目背景

随着企业知识资产的数字化进程加速，如何高效管理和利用海量文档知识成为企业面临的重要挑战。传统的关键词搜索已无法满足用户对语义理解和智能问答的需求。本项目旨在为企业提供一套完整的RAG（Retrieval-Augmented Generation）解决方案，帮助企业构建私有知识库，实现智能语义搜索和问答。

### 1.2 项目定位

**项目名称**：knowledge-rag

**项目定位**：面向企业的私有知识库RAG服务

**核心价值**：
- 多租户隔离：每个企业/团队拥有独立的数据库，物理隔离确保数据安全
- 语义理解：基于bge-m3模型实现深度语义理解
- 快速部署：开箱即用，支持Docker一键部署
- 灵活扩展：支持水平扩展，满足不同规模企业需求

### 1.3 项目目标

| 目标 | 描述 | 优先级 |
|------|------|--------|
| M1 | 支持文档上传、分块、向量化存储 | P0 |
| M2 | 支持语义搜索和混合检索 | P0 |
| M3 | 支持RAG问答功能 | P1 |
| M4 | 支持多应用(appId)隔离 | P1 |
| M5 | 支持Docker部署 | P0 |

---

## 二、用户群体

### 2.1 主要用户

| 用户类型 | 使用场景 | 核心需求 |
|----------|----------|----------|
| 企业用户 | 构建私有知识库 | 数据隔离、安全可靠 |
| 开发团队 | 集成到自有系统 | 简单API、快速集成 |
| 知识管理员 | 管理企业文档 | 批量导入、智能检索 |

### 2.2 用户痛点

1. **数据孤岛**：企业内部文档分散，难以统一检索
2. **搜索不准**：传统关键词匹配无法理解语义
3. **问答困难**：找不到准确答案，需要人工客服
4. **数据安全**：敏感数据不敢上传到第三方服务

---

## 三、功能需求

### 3.1 核心功能模块

#### 3.1.1 应用管理

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 应用注册 | 创建应用，生成appId和appSecret | P0 |
| 应用查询 | 获取应用信息 | P1 |
| 应用禁用 | 禁用/启用应用 | P2 |

#### 3.1.2 文档管理

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 创建文档 | 上传文档（标题、内容、标签） | P0 |
| 文档列表 | 分页查询文档 | P0 |
| 文档详情 | 获取单个文档内容 | P0 |
| 删除文档 | 删除文档及关联向量 | P0 |
| 文档标签 | 支持自定义标签分类 | P1 |

#### 3.1.3 知识检索

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 语义搜索 | 基于向量的相似度搜索 | P0 |
| 混合检索 | 向量+关键词组合检索 | P1 |
| 分页支持 | 支持结果分页 | P0 |

#### 3.1.4 智能问答

| 功能 | 描述 | 优先级 |
|------|------|--------|
| RAG问答 | 基于检索结果生成回答 | P1 |
| 引用来源 | 返回参考文档和原文片段 | P1 |
| 会话管理 | 支持多轮对话 | P2 |

### 3.2 功能详细描述

#### 3.2.1 文档创建流程

用户输入 → 内容解析 → 智能分块 → 向量化 → 存储向量 → 返回结果

1. **内容解析**：支持纯文本、Markdown格式
2. **智能分块**：按段落分块，每块建议512token
3. **向量化**：使用bge-m3生成1024维向量
4. **存储**：向量存入pgvector，文本存入PostgreSQL

#### 3.2.2 语义搜索流程

用户查询 → 向量化 → 向量检索 → 结果排序 → 返回结果

1. **向量化**：将用户查询转为向量
2. **向量检索**：使用pgvector的HNSW索引进行近似搜索
3. **结果排序**：按相似度分数排序
4. **返回结果**：包含原文片段和来源信息

#### 3.2.3 RAG问答流程

用户问题 → 向量化 → 检索知识库 → 构建Prompt → LLM生成 → 返回答案

1. **问题向量化**：将用户问题转为向量
2. **知识检索**：从向量库检索相关文档
3. **Prompt构建**：将检索结果和问题组合成Prompt
4. **LLM生成**：调用大模型生成回答
5. **返回结果**：包含答案和引用来源

---

## 四、非功能需求

### 4.1 性能需求

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 文档创建 | <3s/千字 | 包含向量化时间 |
| 语义搜索 | <500ms | P99延迟 |
| 并发支持 | 50 QPS | 单实例支持 |
| 冷启动 | <30s | 模型首次加载 |

### 4.2 可用性需求

| 指标 | 目标值 |
|------|--------|
| 服务可用性 | 99.9% |
| 数据持久化 | PostgreSQL本地存储 |
| 故障恢复 | 支持重启后自动恢复 |

### 4.3 安全需求

| 需求 | 描述 |
|------|------|
| 身份认证 | appId + appSecret 校验 |
| 数据隔离 | 不同appId使用独立数据库 |
| 传输安全 | 支持HTTPS（生产环境） |
| 敏感信息 | appSecret加密存储 |

### 4.4 扩展性需求

| 需求 | 描述 |
|------|------|
| 水平扩展 | 支持多实例部署 |
| 垂直扩展 | 支持更大内存运行更大模型 |
| 存储扩展 | 支持连接远程PostgreSQL |

---

## 五、用户流程

### 5.1 整体流程图

注册应用(获得凭证) --> 上传文档(知识入库) --> 智能问答(使用知识)

### 5.2 详细流程

#### 流程1：应用注册

1. 管理员在数据库创建应用记录
2. 系统生成appId（固定格式）和appSecret（随机）
3. 返回应用凭证给管理员

#### 流程2：文档管理

1. 用户调用创建文档API
2. 系统自动分块、向量化
3. 向量存储到对应app的数据库
4. 返回文档ID和分块数量

#### 流程3：知识问答

1. 用户调用问答API（携带appId）
2. 系统从对应数据库检索
3. 返回检索结果
4. （可选）调用LLM生成回答

---

## 六、验收标准

### 6.1 功能验收

| 功能 | 验收条件 |
|------|----------|
| 应用创建 | 能够创建应用并获取appId/appSecret |
| 文档创建 | 文档创建成功，向量自动生成 |
| 文档列表 | 分页返回正确，数据完整 |
| 文档删除 | 删除后向量同步清理 |
| 语义搜索 | 返回相关文档，相似度合理 |
| 问答功能 | 能够基于知识库回答问题 |

### 6.2 性能验收

| 指标 | 验收条件 |
|------|----------|
| 创建文档 | 1000字文档创建<3秒 |
| 语义搜索 | P99延迟<500ms |
| 并发测试 | 50QPS下成功率>99% |

### 6.3 安全验收

| 指标 | 验收条件 |
|------|----------|
| 凭证校验 | 无效凭证返回401 |
| 数据隔离 | 不同appId无法访问对方数据 |
| 密码存储 | appSecret加密存储 |

---

## 七、里程碑

| 里程碑 | 交付物 | 预计时间 |
|--------|--------|----------|
| M1 | 基础框架、认证模块、文档CRUD | 1周 |
| M2 | 向量化服务、语义搜索 | 1周 |
| M3 | RAG问答、多租户隔离 | 1周 |
| M4 | Docker部署、文档完善 | 1周 |

---

## 八、风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| 模型加载慢 | 首次请求延迟高 | 使用懒加载+预热 |
| 向量检索不准 | 搜索结果差 | 调优chunk策略和top_k |
| 大模型响应慢 | 问答超时 | 添加超时控制和重试 |
